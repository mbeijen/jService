name: Deploy to VPS

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy files
        run: |
          # Sync directory contents to target location, preserving bundled gems
          rsync -av --delete --exclude=vendor/bundle . /srv/jservice
          cd /srv/jservice
          bundle install --path vendor/bundle

